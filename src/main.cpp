#include <iostream>
#include <vector>

struct A
{
    A(int a_, int b_) : a(a_), b(b_) {}

    int a;
    double b;
};

int main()
{
//    std::vector<A> v(5, NULL);
//    v[0] = A{1, 2};
//    v.emplace_back(1, 2);
    return 0;
}